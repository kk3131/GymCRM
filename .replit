run = "streamlit run app.py --server.port 3000 --server.address 0.0.0.0"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "streamlit run app.py --server.port 3000 --server.address 0.0.0.0"]
